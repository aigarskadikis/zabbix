# access windows UNC share. please note that share will be accessed with the user [SYSTEM by default] which the service "Zabbix Agent" are running
# in order to execute this user parameter [UnsafeUserParameters=1] must be active
UserParameter=path.exists[*],if exist "$1" (echo 1) else echo 0
