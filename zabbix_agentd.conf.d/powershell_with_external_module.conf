# this command will represent how to load external module inside powershell and then run command. Timeout=30 may be required to succeed with this approach
UserParameter=installed.updates.via.powershell,powershell -nologo -ExecutionPolicy Bypass -command "Import-Module C:\zabbix\zabbix_agentd.conf.d\Get-WUList.ps1; Get-WuList -IsInstalled | sort KB | ft KB,Title -AutoSize"
